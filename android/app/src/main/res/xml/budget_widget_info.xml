<?xml version="1.0" encoding="utf-8"?>
<!-- updatePeriodMillis=0: figures only change while the app runs (the Dart
     side pushes updates through the method channel), so periodic launcher
     wakeups would redraw identical data. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:configure="com.fabletest.expense_tracker.BudgetWidgetConfigActivity"
    android:description="@string/budget_widget_description"
    android:initialLayout="@layout/budget_widget"
    android:minWidth="250dp"
    android:minHeight="40dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:previewLayout="@layout/budget_widget"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
